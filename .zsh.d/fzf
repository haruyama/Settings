# Setup fzf
# ---------
if [[ $(fzf --version) > '0.13' ]] ; then
    export FZF_DEFAULT_OPTS="--extended --cycle --select-1 --exit-0 --exact --ansi"
else
    export FZF_DEFAULT_OPTS="--extended --cycle --select-1 --exit-0"
fi

[[ -f "$HOME/.fzf.zsh" ]] && source "$HOME/.fzf.zsh"
